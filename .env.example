# LPCL 本地开发密钥配置（复制为 .env 使用；.env 已 gitignore，绝不提交）
#
# CurseForge API key — 本地开发构建嵌入（Debug 默认 ON）；
#   官方申请: https://console.curseforge.com/
#   留空则 CF 请求自动走 MCIM 镜像（无需鉴权）
LPCL_CURSEFORGE_API_KEY=
#
# 微软 OAuth Client ID — 设备码登录（lpcl login）必需，所有构建都会嵌入
#   这不是秘密（授权页地址栏可见），但每个项目必须注册自己的：
#   1. https://portal.azure.com → Microsoft Entra ID → 应用注册 → 新注册
#   2. 受支持账户类型选「任何组织目录中的账户和个人 Microsoft 账户」
#   3. 注册后在「身份验证」里开启「允许公共客户端流」（设备码流程必需）
#   4. 把「应用程序(客户端) ID」填到下面
# 留空时 lpcl login 不可用（GUI 微软登录同样不可用）
LPCL_MS_CLIENT_ID=
